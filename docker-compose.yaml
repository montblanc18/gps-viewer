version: "3.8"

services: 
    localstack:
        container_name: "${LOCALSTACK_DOCKER_NAME-localstack_main}"
        image: localstack/localstack:latest
        environment: 
            - SERVICES=kinesis, lambda, apigateway, dynamodb
            - DEFAULT_REGION=ap-northeast-1
        ports:
            - 4566:4566